import { SectionContainer } from "../layout";
import { SectionTitle } from "../ui";

export const Team = () => {
  return (
    <SectionContainer>
      <SectionTitle align={"right"}>Team</SectionTitle>
    </SectionContainer>
  );
};
